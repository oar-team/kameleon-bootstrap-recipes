
- do_upgrade:
  - exec_in: pacman -Syy --noconfirm --force
  - exec_in: pacman -Su --noconfirm --force

- do_install:
  - exec_in: pacman --noconfirm --arch $$arch -Suy --force $$setup_packages
  - on_clean:
    - exec_in: echo "y" | pacman -Scc
