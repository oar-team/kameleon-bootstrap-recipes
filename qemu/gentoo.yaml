#==============================================================================
# vim: softtabstop=2 shiftwidth=2 expandtab fenc=utf-8 cc=81 tw=80
#==============================================================================
#
# DESCRIPTION: Gentoo base system built with qemu-kvm.
#
#==============================================================================
---
extend: base.yaml

# Global variables use by Kameleon engine and the steps
global:
  # Distribution
  distrib: gentoo


  ## System variables. Required by kameleon engine
  # Include specific steps
  include_steps:
    - $$distrib

  qemu_memory_size: 2048
  iso_filename: debian-jessie-$${arch}-live.iso
  iso_url: http://kameleon.imag.fr/iso/$$iso_filename
  iso_path: $$kameleon_cwd/$$iso_filename

  autoinstall_script_path: $$kameleon_data_dir/$${distrib}-autoinstall.sh
  boot_timeout: 15

bootstrap:
  - "@base"

# Install and configuration steps
setup:
  - "@base"

# Export the generated appliance in the format of your choice
export:
  - "@base"
